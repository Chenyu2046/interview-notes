## 01. 滑动窗口

### 一类典型题目的特征：

- 输入一个大字符串 `s` 和一个模式字符串 `p`。
    
- 让你找：`s` 中所有满足某种条件的子串。
    
    - 比如：所有异位词、最长无重复子串、最小覆盖子串等。
        

---

###  模板框架

```cpp
unordered_map<char, int> need, window;

for (char c : t) need[c]++; // 预处理目标需求

int left = 0, right = 0;
int valid = 0;

while (right < s.size()) 
{
    char c = s[right];
    right++; // 扩大窗口

    // 进行窗口内数据的一系列更新
    if (need.count(c)) 
    {
        window[c]++;
        if (window[c] == need[c]) 
        {
            valid++;
        }
    }

    // 判断左侧窗口是否要收缩（通常是 >= t.size()）
    while (满足收缩条件) 
    {
        // 在收缩前，判断是否要记录答案
        if (valid == need.size()) 
        {
            // 记录结果，如 left 或 right-left 等
        }

        // d 是将移出窗口的字符
        char d = s[left];
        left++;

        // 进行窗口内数据的一系列更新
        if (need.count(d)) 
        {
            if (window[d] == need[d]) 
            {
                valid--;
            }
            window[d]--;
        }
    }
}
```

---

###  各部分详解

|模块|说明|
|---|---|
|`need`|记录「目标子串」中每个字符/元素需要的个数|
|`window`|记录「当前滑动窗口」中每个字符/元素的个数|
|`valid`|记录窗口中，满足 need 的字符种类有多少种（可用于判断是否符合要求）|
|`right++`|右指针向右扩展窗口|
|`left++`|左指针向右收缩窗口|
|`if(valid == need.size())`|说明窗口中所有字符都满足条件，可以记录答案|
|`while(...)`|控制收缩窗口的时机（比如当窗口长度等于 p.size()）|

---

### 应用要点

#### 1. **什么时候更新 valid？**

- 只有当 `window[c] == need[c]` 时才加 `valid`，因为这表示字符 c 的数量恰好满足要求。
    

#### 2. **什么时候可以收缩窗口？**

- 对于**固定长度**的需求（比如异位词）：`right - left >= p.size()`。
    
- 对于**最小长度**、**可变长度**（比如最小覆盖子串）：可能需要使用 `while (valid == need.size())` 来收缩。
    

#### 3. **结果怎么记录？**

- 异位词：记录 `left`。
    
- 最小覆盖子串：记录窗口长度、起始索引等。
    

---

###  套路识别技巧

|题目类型|滑窗特征|窗口大小|是否使用 valid|
|---|---|---|---|
|找所有异位词|固定窗口 + 频次匹配|固定长度|✅|
|最小覆盖子串|满足所有目标字符，窗口最小|可变长度|✅|
|最长无重复子串|所有字符唯一，最长长度|可变长度|❌（用set/map）|
|子数组满足条件计数|窗口中元素满足某种数量关系|可变长度|有时不需要|

---

### 📚 模板题目推荐（适合练熟套路）

1. LeetCode 438. [找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/) ✅
    
2. LeetCode 76. [最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring/) ✅
    
3. LeetCode 567. [字符串的排列](https://leetcode.cn/problems/permutation-in-string/) ✅
    
4. LeetCode 3. [无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/) ❌（不用 valid）
    

---

###  最后小结口诀（记住这四句话）

> - 构建 need 统计 map
>     
> - 滑动 right 扩窗口（什么时候扩大窗口？）
>     
> - 更新 window + valid
>     
> - 满足条件收缩 left，检查并记录结果（什么时候缩小窗口？）
>     

---


**针对卖家三大主张的理性反驳**  
1️⃣ **屏幕原装性存疑**  
→ 卖家称"有指纹=原装屏"：指纹功能是基础技术，无法证明屏幕来源（仿制屏亦可实现）  
→ **关键矛盾**：屏幕存在边缘漏光、触摸断触、高刷失效问题（附视频），若为原装屏应无此类缺陷  
→ 法律要求：请卖家提供 **苹果官方检测报告** 或 **屏幕-机身匹配序列号**，否则承担举证责任  

1.摄像头故障属故意隐瞒 
卖家承认"未注意到杂质"：但镜头歪斜、蓝光损坏、黑边异常，内部进灰均属肉眼可见缺陷  
这违反了《电子商务法》相关法规，未全部披露所卖商品的问题  

2.后盖缝隙归咎天气不成立
相关报告提到，常见手机密封胶最差耐温-20℃~70℃，常温溢胶=工艺缺陷  ，这些都是肉眼能见到的，但是卖家刻意隐瞒瑕疵
 
   - 后盖缝隙大到可插入4张A4纸  
   - 手指按压有明显位移  
其次：若真是运输导致缝隙，属卖家包装责任（《民法典》604条）  

3.**屏幕原装性存疑**  
卖家对该屏幕是原装的陈述，并不足以证明屏幕是原装的


卖家隐瞒多项重大缺陷，若知情不会购买。
