### 01. Redis 为什么这么快？
`主要是redis他是内存操作+高效的数据结构+单线程避免了锁的竞争+I/O的多路复用减少了网络的开销`
- redis 的数据主要存放在内存里面，少了很多读写磁盘的过程
- redis 内部对很多的数据结构做了优化，所以很多的操作本身复杂度就很低 
- 其次 redis 的核心操作是单线程，避免了多线程下的锁的竞争、线程切换的额外开销
- redis 通过epoll 这类机制，使得一个线程可以应对多个网络链接，这样一来效率也高了

### 02. 单线程的 Redis 为什么能处理高并发？
`redis 能抗高并发重点并不在于cpu，主要是靠 内存访问和网络IO`
**具体的答案和 01 差不多**

### 03. Redis 的 IO 多路复用介绍下？
`本质上就是用一个线程去监听多个socket链接上的事件，哪个链接好了就先处理那个`
redis 下的IO多路复用，我理解的就是一个线程去处理多个socket链接，它会把这些连接统一注册到多路复用器上，像 常见的epoll 。这样一来redis不需要为每个连接都创建线程，而是谁有事件了，就处理谁

### 04.Redis 为什么不设置多线程操作数据？
`核心是：单线程已经足够快了，设置多线程可能还会导致锁竞争，线程切换等行为导致增加复杂度`
redis 本身就是内存型数据库，大部分的命令都很快，核心主要就是网络IO和内存访问，这种情况下如果设置成多线程，收益可能没有那么大

### 05. Redis 6.0 是什么线程的？
- 核心命令还是单线程的，但是在网络IO这块引入了多线程，也就是单线程处理数据，多线程处理网络

### 06.Redis 是单进程还是单线程？哪些模块是单线程？
- redis 是单进程架构，但不是严格的单线程。单线程的时候 主要是处理核心命令的执行相关的操作。在redis 6.0之后，像是关于网络IO，持久化等操作 会通过子线程去进行操作

### 07. 你用过 Redis 哪些数据结构？
`String、Hash、List、Set、ZSet`
- String
	最常用，适合做普通缓存、计数器、分布式锁这类场景。
- Hash
	适合存对象类型的数据，比如用户信息、商品信息、配置数据。
- List
	适合做有序列表，也可以用来实现简单消息队列。
- Set
	适合做去重、标签集合、共同关注这类无序集合场景。
- ZSet
	带分数排序，适合做排行榜、延时队列、按时间线排序的数据。



### 08.介绍下 Redis 中 Zset 的数据结构，ZADD 命令时间复杂度是多少？
1. Redis 的 ZSet 本质上是一个 **有序集合**，每个元素都会带一个 `score`，然后按 `score` 排序。  
	它底层一般是 **跳表 + 字典** 一起实现的：
	- **字典**：用来根据 member 快速找到对应的 score，查找效率高。
	- **跳表**：用来维护分数顺序，方便做范围查询、排序、获取前几名这类操作。
2. `ZADD` 的时间复杂度通常是 **O(logN)**。  
	因为它本质上要把元素插入到跳表合适的位置，同时更新字典。  
	如果一次插入多个元素，那就是 **O(M logN)**，`M` 是插入元素个数。

### 09.介绍下 Redis 数据库类型和存储结构？
- **Redis 常用数据类型**  
    String、Hash、List、Set、ZSet。
- **存储结构**  
    Redis 对外提供的是这些数据类型，对内其实是不同的底层结构实现。  
    比如：
    - String：`int`、`embstr`、`raw`
    - Hash：压缩结构或 `hashtable`
    - List：`quicklist`
    - Set：`intset` 或 `hashtable`
    - ZSet：`skiplist` + `dict`

### 10.Redis 存 String 转 map 和直接存 map 的区别？
- **string 转 map 存**  
    一般就是把对象序列化成 JSON，整体作为一个 String 存进去。  
    好处是 **实现简单、结构完整、适合整体读写**；缺点是 **改一个字段也要整块反序列化再写回去**，不适合频繁更新局部字段。
- **直接存 map**  
    一般指用 Redis 的 **Hash** 来存，每个 field 对应对象的一个属性。  
    好处是 **可以单独读写某个字段**，对局部更新更友好；缺点是 **使用上比 String 稍复杂**，如果对象层级很深，处理起来没 JSON 直观。

### 11. 为什么 Redis 使用的是哈希索引？
核心原因就是 redis 就是用的key-value 进行存储的，通过key去找value，而哈希表在这类场景下查找效率最高，平均时间复杂度能到O（1）。像是redis这种高频读写的，快速定位的数据访问模式。本质就是更加care 单个key 的查询，哈希表就是最合适的选择

### 12. Redis 的 set 集合元素过多怎么办？
`set元素过多会导致删除，遍历过慢的清况`
所以要避免set过大。
1. 进行拆分，按照业务维度相关方面，拆分成多个小set
2. 如果数据量大可以把一个大set拆分成多个桶来存
3. 如果只是为了去重，看看能不能用其他的数据类型
4. 定期清理数据，无效数据过期就清掉

### 13. 介绍下 Redis 集群？
Redis 集群就是 Redis 的**分布式方案**，主要解决两个问题：**数据分片**和**高可用**。它把数据按 **16384 个哈希槽**分布到不同节点上，实现横向扩容；同时每个主节点可以挂从节点，主节点故障后能自动切换，保证服务可用。

### 14. 介绍下 Redis 哨兵模式？
Redis 哨兵模式本质上是 Redis 的一种**高可用方案**，它主要负责**监控主从节点状态**。如果发现主节点挂了，哨兵会自动从从节点里选一个新的主节点，然后通知其他从节点和客户端完成切换。所以你可以把它理解成：**哨兵不存数据，它是专门用来做监控、告警和故障转移的。**

### 15. 哨兵模式中 master 挂了，接下来会做什么事情？
`先确认故障，再选主，再完成主从切换`
- **第一，先判断 master 是不是真的挂了**  
    哨兵会先做主观下线判断，如果多个哨兵都认为这个 master 不可用了，就会把它判定为**客观下线**。
- **第二，选出一个哨兵当 leader**  
    多个哨兵里会先选出一个 leader，由它来负责后面的故障转移流程。
- **第三，从 slave 里选一个新的 master**  
    leader 哨兵会按照一定规则挑一个从节点提升为新的 master，一般会看**优先级、复制偏移量、run id**这些条件。
- **第四，让其他 slave 重新跟新 master 同步**  
    剩下的从节点会改为复制新的 master，重新建立主从关系。
- **第五，通知客户端更新 master 地址**  
    哨兵会把新的 master 信息通知给客户端，客户端后续就会连到新的 master。


### 16. 介绍下 Redis 全量同步和增量同步对应条件？
- **全量同步**  
    一般发生在**第一次主从复制**，或者从节点重连后发现 **replication id 对不上**、缺失的数据已经不在 **backlog buffer** 里了，这时候只能全量同步。
- **增量同步**  
    一般发生在**短暂断线重连**，只要主节点里还保留着从节点缺失那部分数据，就可以只同步增量命令。
- **核心区别**  
    **数据还在 backlog 里就增量，不在就全量。**

### 17. Redis 全量同步时产生的数据放在哪里？
Redis 全量同步时，主节点会先执行 `bgsave`，生成一份 **RDB 快照**。这个数据本质上是先生成成 **RDB 文件**，再发给从节点。早期一般会落到**磁盘**，然后再传输；如果开启了无盘复制，也可以**不落磁盘，直接通过网络发送给从节点**。所以你可以简单理解成：**全量同步的数据本质上就是一份 RDB 快照，默认可能经过磁盘，也可以直接走内存缓冲发出去。**

### 18. Redis 从节点长时间宕机恢复后如何同步？
Redis 从节点长时间宕机后再恢复，一般会先拿自己的 **replication id** 和 **offset** 去找主节点尝试增量同步。但因为宕机时间太长，主节点的 **repl backlog buffer** 里通常已经没有它缺失的那部分数据了，所以**大概率会走全量同步**。也就是主节点重新生成一份 **RDB 快照** 发给从节点，从节点先加载这份快照，再继续同步后续写命令


### 19. Redis 怎么判断从节点网络状况好坏？
Redis 一般会通过**主从心跳**和**复制偏移量**来判断从节点网络状况。主节点会看从节点多久没发 ACK，如果间隔太长，说明网络可能有问题；同时也会看从节点的 **offset** 追得快不快，如果复制延迟越来越大，说明网络传输或者同步状态不好。简单说就是：**看从节点回包是否及时，以及数据同步是否滞后。**


### 20. Redis 的 repl_backlog 需要存储的数据超过容量会发生什么？
`repl_backlog` 本质上是一个**固定大小的环形缓冲区**。如果要存的数据超过它的容量，**旧的数据会被新数据覆盖**。这样做不会让 Redis 报错，但如果某个从节点落后太多，刚好它缺失的数据已经被覆盖掉了，那它下次重连时就**没法做增量同步**，只能走**全量同步**。


### 21. Redis 怎么部署的？
如果是**中小规模**，通常用 **主从 + 哨兵**，主节点负责写，从节点负责读，哨兵负责监控和故障转移，这种方案实现简单。  
如果是**数据量大、并发高**，一般会用 **Redis Cluster**，把数据分片到多个节点上，同时每个主节点再配从节点做高可用。


### 22. 介绍下 Redis 的持久化机制（RDB/AOF）？
- **RDB**  
    RDB 是**快照持久化**，就是在某个时间点把当前内存里的数据整体保存成一份 dump 文件。  
    优点是**恢复快、文件紧凑、适合备份**；缺点是如果 Redis 异常宕机，**两次快照之间的数据可能会丢**。
- **AOF**  
    AOF 是**追加日志持久化**，每次写命令都会按协议追加到 AOF 文件里，恢复时再把这些命令重新执行一遍。  
    优点是**数据更安全，丢失更少**；缺点是 **AOF 文件通常更大，恢复速度也比 RDB 慢**。
- **怎么选**  
    如果更看重**恢复速度和备份**，偏向 RDB；  
    如果更看重**数据完整性**，偏向 AOF；  
    实际上很多场景会**RDB + AOF 一起用**。


### 23. Redis 在 AOF 文件太大会启动重写，重写做了什么？
AOF 重写本质上不是把旧 AOF 简单压缩一下，而是**根据当前内存里的数据，重新生成一份新的 AOF 文件**。***它会把那些重复、冗余的历史写命令去掉，只保留能恢复当前结果的最少命令*。**比如一个 key 被 `set` 了很多次，重写后可能只保留最后一次有效的 `set`。这样做的目的就是**减小 AOF 文件体积，提升恢复效率**。


### 24. Redis 内存不够怎么办？
- **先配淘汰策略**  
    比如 LRU、LFU，让 Redis 在内存打满时自动淘汰一部分数据。
- **再做数据优化**  
    清理无效数据、拆分大 key、压缩 value，减少不必要的内存占用。
- **换更省内存的数据结构**  
    比如部分场景下用 Bitmap、HyperLogLog 代替 Set。
- **最后再扩容**  
    如果业务量本身就大，就加机器或者上 Redis Cluster 做分片。
- **核心就是**  
    **能省先省，不够再扩。**


### 25. 介绍下 Redis 过期策略？
**关键字：惰性删除、定期删除**

- **惰性删除**  
    key 过期了，不会立刻删，而是在**访问这个 key 的时候**再检查它有没有过期，过期了就删除。
- **定期删除**  
    Redis 会**周期性随机抽取一部分设置了过期时间的 key** 做检查，发现过期就删除，避免大量过期 key 一直占内存。
- **为什么这样设计**  
    如果只靠惰性删除，很多过期 key 可能一直没人访问，会浪费内存；  
    如果只靠定期删除，又不可能每次都全量扫描，成本太高。  
    所以 Redis 采用的是**惰性删除 + 定期删除**结合的方式。
- **补一句**  
    如果过期 key 还是很多，内存又满了，还会再配合**内存淘汰策略**一起工作。


### 26. Redis 的 AOF 遵循 WAL 原则吗?
Redis 的 AOF **可以说遵循了 WAL 的思想**，因为它也是**先把写命令追加到日志，再根据日志恢复数据**。但它和传统数据库里严格意义上的 WAL 还是有区别。Redis AOF 记录的是**写命令本身**，不是数据页的物理修改；而且它是为了**持久化恢复**，不是像 MySQL WAL 那样主要服务于事务和崩溃恢复。所以面试里比较稳的说法是：**Redis AOF 借鉴了 WAL 的思想，但实现方式和典型数据库 WAL 不一样。**

### 27. Redis 分布式锁实现方式及使用场景？
**关键字：SET NX EX、互斥控制**

**实现方式**
一般用 SET key value NX EX time 实现。
NX 保证只有一个线程能加锁，EX 防止死锁，value 一般放唯一标识。
释放锁
不能直接 del，要先判断 value 是不是自己的，一般用 Lua 脚本保证原子性。
使用场景
主要用在分布式环境下的并发控制，比如防重复下单、库存扣减、定时任务只能执行一次。


### 28.如何避免 Redis 缓存击穿问题？
`缓存击穿是指一个极热点key失效的瞬间，有大量的并发请求直接打到数据库。解决的核心在于控制只有一个线程去查库回填（只放一个请求过去重建缓存）`
- **加互斥锁**  
    热点 key 失效时，只让一个线程去查数据库并回填缓存，其他线程等待，这样避免大量请求同时打到 DB。
- **热点数据不过期**  
    对特别热点的数据，可以不设置过期时间，或者由后台线程主动更新，避免在高并发时突然失效。
- **提前预热**  
    监控系统识别出热点 Key，在它们即将过期前，通过后台脚本人工或自动进行续期。


### 29. 介绍下 Redis 雪崩解决方法？
`redis 雪崩是指同一时间内大量的key同时过期，或者整个redis服务宕机，导致所有的请求都打到数据库上`
`Redis 雪崩的核心就是：**避免大量缓存同时失效，再加上后端兜底保护。**`
- **过期时间打散**  
    不要让大量 key 在同一时间一起过期，TTL 可以加一个随机值，避免同一时刻大量请求同时穿到数据库。
- **热点数据预热**  
    对核心热点数据提前加载到缓存里，或者后台主动续期，减少大面积失效的概率。
- **加限流和降级**  
    真发生雪崩时，要保护数据库，比如限流、熔断、降级，避免数据库被瞬间打垮。
- **做多级缓存**  
    本地缓存 + Redis 一起用，就算 Redis 短时间异常，也不至于所有请求都直接打到 DB。

### 30. 介绍下 Redis 锁误删和锁超时解决办法？
- **锁误删怎么解决**  
    加锁时 value 不能随便写，一般要放 **唯一标识**，比如线程 ID 或请求 ID。  
    释放锁时不能直接 `del`，要先判断这个锁是不是自己的，再删除。这个过程一般用 **Lua 脚本**保证原子性，避免把别人的锁删掉。
- **锁超时怎么解决**  
    加锁时一定要带 **过期时间**，防止服务异常退出后锁一直不释放。  
    如果业务执行时间可能超过锁时间，就要加 **续期机制**，也就是业务还没执行完时，自动给锁延长过期时间。


### 31. 如果 Redis 锁在多机器场景下挂了怎么办？
- **第一，做高可用部署**  
    至少不要单机，通常会用主从 + 哨兵或者集群，先保证 Redis 服务本身可用。
- **第二，接受单点锁的局限性**  
    很多业务里其实不会强依赖 Redis 锁做到绝对强一致，而是把它当一个高性能互斥手段。
- **第三，用多节点方案**  
    比如 **RedLock**，在多台独立 Redis 上加锁，只有大多数节点成功才算加锁成功，这样单台挂掉影响会小一些。
- **第四，业务层兜底**  
    真正关键的场景，不能只靠 Redis 锁，还要加**幂等、状态机、唯一约束**这些业务保护。



### 32. Mysql 和 Redis 怎么保持一致？
`先写 MySQL，再删 Redis`
最常见做法就是 **旁路缓存模式**：读先查 Redis，没有再查 MySQL 并回填；写的时候 **先更新 MySQL，再删除 Redis**。这样下次读请求会从 MySQL 拿最新数据再写回缓存。核心上，MySQL 和 Redis 一般做的是**最终一致性**，不是强一致。


### 33. 如何使用 Redis 实现排行榜功能？
排行榜一般用 Redis 的 **ZSet** 实现，因为它天然支持按 `score` 排序。  
比如用户 ID 当 member，积分当 score，用 `ZADD` 写入分数，查排行榜可以用 `ZREVRANGE` 取前 N 名，查某个用户排名可以用 `ZREVRANK`，查分数可以用 `ZSCORE`。所以它很适合做积分榜、活跃榜、比赛排名这类场景。


### 34. 两个线程同时修改 Redis 同一条数据时怎么增加并发？
如果两个线程同时改同一条 Redis 数据，Redis 本身因为**命令执行是单线程**，不会出现真正的并行修改，所以重点不是“让这一个 key 并发执行”，而是**减少对同一个 key 的竞争**。常见做法有：

- **拆分热点数据**  
    不要所有请求都改同一个 key，可以分片、分桶，先把并发打散。
- **批量或异步合并更新**  
    先把多次修改聚合起来，再一次性写 Redis，减少冲突次数。
- **用 Lua 保证原子更新**  
    如果是读改写场景，可以用 Lua 脚本把逻辑放一起执行，避免并发覆盖。
- **乐观锁控制**  
    可以用 `WATCH + MULTI + EXEC` 做 CAS，冲突了再重试。


### 35. 如何使用 Redis 实现验证码防刷？
验证码防刷一般就是用 Redis 做**频率控制**。最常见做法是按 **手机号、IP、设备号** 这种维度做计数，比如发送验证码前先查这个手机号在 1 分钟内发了几次、1 小时内发了几次，如果超过阈值就直接拒绝。实现上通常是用 `INCR` 配合 `EXPIRE`，第一次请求时设置过期时间，后面每次请求自增计数。这样就能很方便地做 **1 分钟 1 次、1 小时 5 次、1 天 10 次** 这类限制。本质上就是：**用 Redis 的原子计数和过期机制做滑动限制，防止用户短时间内反复刷验证码。**


### 36. 如果 Redis 压力过大，你有哪些优化的手段？
Redis 压力过大，常见优化手段就三类：  
**第一，优化热点**，比如本地缓存、热点拆分、预热；  
**第二，分摊压力**，比如读写分离、Pipeline、减少无效请求；  
**第三，直接扩容**，单机扛不住就上 Redis Cluster。  
核心就是：**先定位瓶颈，再针对性优化，不够再扩**


### 37. 介绍下 Redis 中的 gossip 协议？
Redis 里的 Gossip 协议主要用在 **Cluster 模式** 下，本质上就是**节点之间定期互相交换信息**。比如我是谁、我负责哪些槽、我认为什么节点挂了，这些状态都会通过 Gossip 在集群里传播。这样做的好处是**不需要一个中心节点统一通知**，每个节点都能慢慢感知整个集群状态。面试里可以简单理解成：**Gossip 就是 Redis Cluster 用来做节点发现、状态同步和故障传播的一种通信机制。**


### 38. 介绍下 Redis 的 setnx 命令底层原理？
`SETNX` 的底层原理其实不复杂，本质就是 Redis 先去自己的 **key 字典**里查这个 key 是否存在。

- **如果不存在**，就把这个 key 和 value 写进去，返回 `1`；
- **如果已经存在**，就什么都不做，返回 `0`。

因为 Redis **命令执行是单线程**的，所以这个“判断 key 是否存在 + 插入 key”过程天然就是**原子操作**，不会出现并发下两个线程同时都插入成功的问题。  
所以面试里可以概括成：**SETNX 本质就是基于 Redis 哈希表做一次不存在才写入的原子操作。**


### 39. Redis 事务哪些情况下会失败？
- **命令入队阶段失败**  
    比如语法错误、命令参数不对，这种在 `EXEC` 前就会报错，整个事务不会执行。
- **执行阶段某条命令报错**  
    比如对 String 执行 Hash 命令，这种是运行时错误。  
    但 Redis 不会像关系型数据库那样整体回滚，**其他命令还是会继续执行**。
- **WATCH 监控的 key 被改了**  
    如果事务执行前，`WATCH` 的 key 被别的客户端改动了，那 `EXEC` 会直接失败，事务不会提交。
- **核心就是**  
    Redis 事务常见失败场景就两类：**入队就失败**，或者 **WATCH 冲突导致提交失败**；如果只是执行期某条命令报错，**不会整事务回滚**。



### 40. Redis 中的信息是加密的还是已经解密的？
Redis 里的数据**默认是明文**，不管是内存里的数据，还是客户端和 Redis 之间的普通通信，默认都不是加密的。  
如果要加密，通常是两种方式：

- **传输加密**：开启 **TLS**，保护客户端和 Redis 之间的数据传输
- **存储加密**：一般要靠磁盘层、文件系统层，或者云厂商提供的加密能力，Redis 本身不会默认把数据自动加密

所以面试里可以直接答：**Redis 默认不是加密存储，数据通常是明文；如果有安全要求，一般靠 TLS 做传输加密，靠底层存储方案做落盘加密。**


